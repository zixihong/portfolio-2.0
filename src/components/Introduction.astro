---
---

<div class="intro">
    <p>
        “i saw my life branching out before me like the <span class="green-text">green fig tree</span> in the story. from the tip of every branch, like a <span class="purple-text">fat purple fig</span>, a wonderful future beckoned and winked. one fig was a husband and a happy home and children, and another fig was a famous poet and another fig was a brilliant professor, and another fig was ee gee, the amazing editor, and another fig was europe and africa and south america, and another fig was constantin and socrates and attila and a pack of other lovers with queer names and offbeat professions, and another fig was an olympic lady crew champion, and beyond and above these figs were many more figs i couldn't quite make out.”
    </p>
    <p>
        <b>- sylvia plath, <a href="https://www.goodreads.com/quotes/7511-i-saw-my-life-branching-out-before-me-like-the" target="_blank" rel="noopener noreferrer" title="View quote on Goodreads">the bell jar</a></b>
    </p>
</div>

<script>
    (function initTooltip() {
        const tooltip = document.createElement('button');
        tooltip.innerHTML = 'check out my hand-drawn fig tree by clicking on the figs!';
        
        tooltip.style.cssText = `
            position: absolute;
            left: 1rem;
            top: 1rem;
            background: rgba(0, 0, 0, 0.5);
            color: #fff;
            padding: 0.5rem 1rem;
            border-radius: 8px;
            max-width: 400px;
            font-size: 0.8rem;
            line-height: 1.2;
            display: block;
            z-index: 1000;
            border: none;
            cursor: pointer;
        `;

        const tooltipRight = document.createElement('button');
        tooltipRight.innerHTML = 'want to know how i made this? ↗';
        
        tooltipRight.style.cssText = `
            position: absolute;
            right: 1rem;
            top: 1rem;
            background: rgba(0, 0, 0, 0.5);
            color: #fff;
            padding: 0.5rem 1rem;
            border-radius: 8px;
            max-width: 400px;
            font-size: 0.8rem;
            line-height: 1.2;
            display: block;
            z-index: 1000;
            border: none;
            cursor: pointer;
        `;

        const mediaQuery = window.matchMedia('(max-width: 768px)');
        const handleMobileChange = (e: MediaQueryListEvent | MediaQueryList) => {
            if (e.matches) {
                tooltip.style.display = 'none';
                tooltipRight.style.display = 'none';
            } else {
                tooltip.style.display = 'block';
                tooltipRight.style.display = 'block';
            }
        };
        
        mediaQuery.addEventListener('change', handleMobileChange);
        handleMobileChange(mediaQuery); // Initial check
        tooltip.addEventListener('click', () => {
            window.open('https://github.com/zixihong/portfolio-2.0', '_blank');
        });

        tooltipRight.addEventListener('click', () => {
            window.open('https://github.com/zixihong/svg-path-collector', '_blank');
        });

        document.body.appendChild(tooltip);
        document.body.appendChild(tooltipRight);
    })();
</script>

<style>
    .intro {
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        width: 80%;
        margin: 0 auto;
        font-size: clamp(1rem, 3vw, 1.2rem);
        line-height: 1.5;
        font-family: 'Space Grotesk', sans-serif;
        font-weight: 400;
        color: #000000;
    }
    .green-text {
        color: #00ff00;
    }
    .purple-text {
        color: #aa00ff;
    }

    @media (max-width: 768px) {
        .intro {
            width: 90%;
            padding: 1rem;
        }
        .intro p {
            font-size: 0.9rem;
            line-height: 1.4;
        }
    }

    .intro-text {
        font-size: 1.5rem;
        line-height: 1.6;
        margin-bottom: 2rem;
        text-align: center;
    }

    @media (min-width: 768px) {
        .intro-text {
            text-align: left;
        }
    }

    .intro a {
        color: inherit;
        text-decoration: none;
    }

    .intro a:hover {
        text-decoration: underline;
    }
</style>

